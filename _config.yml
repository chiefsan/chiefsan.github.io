# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: "Sanjay Seetharaman"
email: 'sanjayms0111@gmail.com'
description: "My personal homepage"
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://chiefsan.github.io" # the base hostname & protocol for your site, e.g. https://example.com
fullurl: "https://chiefsan.github.io"
github_username: 'chiefsan'
linkedin_username: 'sanjayseetharaman'
home_section_title: 'Programming Projects'
home_section_subtitle: 'Projects'
author:
  name: Sanjay Seetharaman
  image: "/assets/images/profile.PNG"
  social:
    name: Sanjay Seetharaman
    links:
      - https://www.linkedin.com/sanjayseetharaman
      - https://github.com/chiefsan
# Build settings
markdown: kramdown
plugins:
  - jekyll-paginate-v2
  - jekyll-sitemap
  - jekyll-seo-tag

environment: production

# Enable future posts (this is to track a bug with --drafts causing issues)
future: true

# Due to us using the categories for posts then it is advisable to explicitly specify a 
# permalink structure for the site. Otherwise Jekyll attempts to organize the pages in folders
# by the category names and it doesn't understand multi-categories to well :/
# permalink: /:year/:month/:title.html
#  permalink: pretty

# Pagination Settings
pagination:
  enabled: true
  per_page: 10
  permalink: '/page/:num/'
  title: ':title - page :num'
  limit: 0
  sort_field: 'date'
  sort_reverse: true

collections_dir: collections

collections:
  misc:
    output: true
    permalink: /misc/:path/
  posts:
    output: false

autopages:
  enabled: true
  tags: 
    layouts: 
      - autopage_collections_tags.html
    enabled: true
  collections:
    enabled: true
    slugify:
      mode: 'ascii'
      cased: false
    permalink: ':coll'
    title: 'Posts in :coll'

permalink: pretty

# ############################################################
# # Site configuration for the Jekyll 3 Pagination Gem
# # The values here represent the defaults if nothing is set
# pagination:
  
#   # Site-wide kill switch, disabled here it doesn't run at all 
#   enabled: true

#   # Set to 'true' to enable pagination debugging. This can be enabled in the site config or only for individual pagination pages
#   debug: false

#   # The default document collection to paginate if nothing is specified ('posts' is default)
#   collection: 'posts'

#   # How many objects per paginated page, used to be `paginate` (default: 0, means all)
#   per_page: 10

#   # The permalink structure for the paginated pages (this can be any level deep)
#   permalink: '/page/:num/' # Pages are index.html inside this folder (default)
#   #permalink: '/page/:num.html' # Pages are simple html files 
#   #permalink: '/page/:num' # Pages are html files, linked jekyll extensionless permalink style.

#   # Optional the title format for the paginated pages (supports :title for original page title, :num for pagination page number, :max for total number of pages)
#   title: ':title - page :num'

#   # Limit how many pagenated pages to create (default: 0, means all)
#   limit: 0
  
#   # Optional, defines the field that the posts should be sorted on (omit to default to 'date')
#   sort_field: 'date'

#   # Optional, sorts the posts in reverse order (omit to default decending or sort_reverse: true)
#   sort_reverse: true

#   # Optional, the default category to use, omit or just leave this as 'posts' to get a backwards-compatible behavior (all posts)
#   category: 'posts'

#   # Optional, the default tag to use, omit to disable
#   tag: ''

#   # Optional, the default locale to use, omit to disable (depends on a field 'locale' to be specified in the posts, 
#   # in reality this can be any value, suggested are the Microsoft locale-codes (e.g. en_US, en_GB) or simply the ISO-639 language code )
#   locale: '' 

#  # Optional,omit or set both before and after to zero to disable. 
#  # Controls how the pagination trail for the paginated pages look like. 
#   trail: 
#     before: 2
#     after: 2

#   # Optional, the default file extension for generated pages (e.g html, json, xml).
#   # Internally this is set to html by default
#   extension: html

#   # Optional, the default name of the index file for generated pages (e.g. 'index.html')
#   # Without file extension
#   indexpage: 'index'

# ############################################################

### Phantom settings
footer_text: '© 2020 Sanjay Seetharaman'
admin_name: 'Sanjay Seetharaman'
google_analytics: "UA-164910723-1" # Update with your own tracking ID
enable_contact: false

#### Phantom Navigation menu
enable_nav: true
nav_item:
  - { url: '/', text: 'Home' }
  - { url: '/cv', text: 'CV' }
#  - { url: '/misc', text: 'Misc' }

# Build settings
compress-site: yes

encoding: "utf-8"
compress_html:
  clippings: all
  ignore:
    envs: development

theme_skin: ceramic

sass:
  style: compressed
